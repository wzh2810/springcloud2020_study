
server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: nacos.wang-zhen.top:8848 #配置Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'